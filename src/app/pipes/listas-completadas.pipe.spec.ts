import { ListasCompletadasPipe } from './listas-completadas.pipe';

describe('ListasCompletadasPipe', () => {
  it('create an instance', () => {
    const pipe = new ListasCompletadasPipe();
    expect(pipe).toBeTruthy();
  });
});
